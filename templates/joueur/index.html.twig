{% extends 'baseBack.html.twig' %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h5 class="card-title mb-0" class="center">Joueur</h5>
        </div>
        <div class="card-body">
            <center>
                <a href="{{ path('ajoutJ') }}"> <button  class="btn btn-primary">Ajout un Joueur</button> </a>
                <br><br>
                <a href="{{ path('afficheJ') }}" ><button  class="btn btn-primary">Liste des Joueurs</button> </a>
                <br><br>
                <a href="{{ path('stajo') }}"> <button class="btn btn-primary btn-sm">statistique</button></a>
            </center>
        </div>
    </div>

{% endblock %}