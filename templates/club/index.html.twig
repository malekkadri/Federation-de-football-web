{% extends 'baseBack.html.twig' %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h5 class="card-title mb-0" class="center">Club</h5>
        </div>
        <div class="card-body">
            <center>
                <a href="{{ path('ajoutCl') }}"> <button  class="btn btn-primary">Ajout un Club</button> </a>
                <br><br>
                <a href="{{ path('afficheCl') }}" ><button  class="btn btn-primary">Liste des clubs</button> </a>
            </center>
        </div>
    </div>

{% endblock %}