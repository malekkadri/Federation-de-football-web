{% extends 'baseBack.html.twig' %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h5 class="card-title mb-0" class="center">Sponsor</h5>
        </div>
        <div class="card-body">
<center>
            <a href="{{ path('ajoutSp') }}"> <button  class="btn btn-primary">Ajout une Sponsor</button> </a>
                    <br><br>
            <a href="{{ path('afficheSp') }}" ><button  class="btn btn-primary">Liste des Sponsors </button> </a>
</center>
        </div>
    </div>

{% endblock %}
