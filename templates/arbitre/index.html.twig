{% extends 'baseBack.html.twig' %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h5 class="card-title mb-0" class="center">Arbitrage</h5>
        </div>
        <div class="card-body">
            <center>
                <a href="{{ path('ajoutAr') }}"> <button  class="btn btn-primary">Ajout un Arbitre</button> </a>
                <br><br>
                <a href="{{ path('afficheAr') }}" ><button  class="btn btn-primary">Liste d'arbitres</button> </a>
            </center>
        </div>
    </div>

{% endblock %}