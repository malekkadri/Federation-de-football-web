{% extends 'baseBack.html.twig' %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h5 class="card-title mb-0" class="center">Match</h5>
        </div>
        <div class="card-body">
            <center>
                <a href="{{ path('ajoutG') }}"> <button  class="btn btn-primary">Ajout une Match</button> </a>
                <br><br>
                <a href="{{ path('afficheG') }}" ><button  class="btn btn-primary">Liste des matchs</button> </a>
            </center>
        </div>
    </div>

{% endblock %}
