{% extends 'baseFront.html.twig' %}
{% block match %}
    <section class="image-header">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <div class="info">
                        <div class="wrap">
                            <ul class="breadcrumbs">
                                <li>Main</a>/</li>
                                <li>Stades</li>
                            </ul>
                            <h1>Stades</h1>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
{% block  title %}


    {% for i in c %}


            <li>
                <a href="{{ asset(path("affiches",{'id':i.id})) }}"><span><img src="{{ asset('uploads/' ~ i.logo) }}" alt="" style="width: 40px" style="height:40px"> {{ i.nomt }} <i class="fa fa-long-arrow-right" aria-hidden="true"></i></span></a>
                <ul>
                    <li><a href="{{ path('afficheGFD',{'id':i.id}) }}"><span>Matches</span></a></li>
                    <li><a href="{{ path('afficheCls',{'id':i.id}) }}"><span>Standings</span></a></li>

                </ul>
            </li>


    {% endfor %}
    {% endblock %}
{% block body %}
   {#  <div class="card flex-fill">
    <div class="card-header">
    <h1>La liste des Stades</h1>

    <table class="table table-hover my-0">
        <tr>

            <td >Name</td>
            <td>Lieu</td>
            <td>Etat</td>
            <td>Nombre</td>
            <td>Photo</td>

        </tr>
        {% for i in e %}
        <tr>

            <td>{{ i.noms }}</td>
            <td>{{ i.lieu }}</td>
            <td>{{ i.etat }}</td>
            <td>{{ i.nbrP }}</td>
            <td><img src="{{ asset('uploads/' ~ i.Photo) }}" alt="" style="width: 80px" style="height:80px"> </td>


            {% endfor %}
        </tr>
    </table>
    </div>
    </div>

    <div class="row">
        {% for i in e %}
        <div class="col-md-4 col-sm-6">

            <div class="store-list-item">
                <div>

                    <img src="{{ asset('uploads/' ~ i.Photo) }}" alt="" style="width: 300px" style="height:300px">
                    </a>
                    <div class="info">
                        <span class="name">{{ i.noms }}</span>

                        <span class="price">{{ i.nbrP }}</span>
                        <div class="btn-wrap">
                            <span class="name"> {{ i.lieu }}</span>
                            <span class="name"> {{ i.etat }}</span>
                        </div>
                    </div>

                </div>
            </div>

        </div>
        {% endfor %}

    </div> #}
    <div class="club-top-info">
    <div class="match-live-date">
    <div class="container">
        <div class="row">
        <div class="foot-left-menu">
            <div class="col-md-6"><div class="date">
        <h5>Stades</h5>
   </div></div>
           <div class="col-md-6"><div class="event">
                               <a href="{{ path('llistpdf') }}">Imprimer</a></div></div>
        </div>
    </div>
</div>
</div>
    <div class="club-staff-top">
    <div class="container">
    <div class="row">
    
        {% for i in e %}
    <div class="staff-box">
    <div class="col-md-4  col-sm-6 col-xs-12">
         <a href="{{ path('detailS', { 'id': i.id }) }}" class="item">
            <span class="info">
                <span class="name">{{ i.noms }}</span>
                <span class="position">Stade</span>
                <span class="number">{{ i.nbrP }}</span>
            </span>
            <img src="{{ asset('uploads/' ~ i.Photo) }}" alt="" style="width: 300px" style="height:300px">
            
</a>

        
    </div>
    </div>
        {% endfor %}
    </div> </div> </div></div>

{#    <div class="store-wrap">
    <div class="container">
        <div class="row row-offcanvas row-offcanvas-left">
    <div class="col-xs-12 col-sm-12 col-md-9">
        <p class="hidden-md hidden-lg">
            <button type="button" class="btn sidebar-btn" data-toggle="offcanvas" title="Toggle sidebar">sidebar</button>
        </p>
        <h6>Stades</h6>
        <div class="row">
            {% for i in e %}
            <div class="col-md-4 col-sm-6">
                <div class="store-list-item">
                    <div>


                        <img  src="{{ asset('uploads/' ~ i.Photo) }}" alt="product" style="width: 600px" style="height:600px">
                        </a>
                        <div class="info">

                            <span class="name">{{ i.noms }}</span>

                            <span class="price">{{ i.nbrP }}</span>
                            <div class="btn-wrap">
                                <span class="name"> {{ i.lieu }}</span>
                                <span class="name"> {{ i.etat }}</span>
                            </div>
                        </div>

                    </div>
                </div>
            </div>

{% endfor %}
        </div>
    </div>
        </div>
    </div>
    </div>
#}

<!--MAIN MATCH SHEDULE END-->





{% endblock %}