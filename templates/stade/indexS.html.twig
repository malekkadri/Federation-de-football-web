{% extends 'baseBack.html.twig' %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h5 class="card-title mb-0" class="center">Stade</h5>
        </div>
        <div class="card-body">
<center>
            <a href="{{ path('ajoutS') }}"> <button  class="btn btn-primary">Ajout une Stade</button> </a>
                    <br><br>
            <a href="{{ path('afficheS') }}" ><button  class="btn btn-primary">Liste des Stades </button> </a>
</center>
        </div>
    </div>

{% endblock %}
